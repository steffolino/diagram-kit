# Changesets

Run `pnpm changeset` after making a change that should ship in a release. It
walks you through picking which packages changed and whether it's a
major/minor/patch bump, then writes a markdown file describing the change.

See https://github.com/changesets/changesets/tree/main/docs/intro-to-using-changesets.md
